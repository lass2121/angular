export interface BookingData {
  dateBooking: string;
  name: string;
  icafeAddress: string;
  pcNumber: string;
  duration: string;
  totalAmount: string;
  status: string;
}

export interface Province {
  provinsi: [];
}

export interface City {
  kota_kabupaten: [];
}
